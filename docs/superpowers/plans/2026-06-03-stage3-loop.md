# Estágio 3: Ponte assíncrona LLM-congelada ↔ controlador-rápido — Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans para implementar tarefa-a-tarefa. Steps usam checkbox (`- [ ]`).

**Goal:** Construir e medir o loop mínimo de duas velocidades — Qwen2.5-0.5B congelada como córtex lento (System 2) emitindo um objetivo-latente que um controlador rápido (System 1) consome todo passo via ponte assíncrona estilo RTC — e produzir a curva **sucesso × latência** que decide se a LLM é co-controladora (A) ou planejador lento (B).

**Architecture:** Processo único, 2 threads. `CortexThread` roda a LLM congelada em background e publica um latente num `LWWRegister`; o loop rápido lê o último latente (possivelmente velho), aplica blend exponencial RTC + zero-order-hold, e age. Backend `stub` (córtex MLP + sleep calibrado) permite TDD no servidor sem baixar a LLM; backend `mps` roda a LLM real no Mac. Reusa `ContextualReach`, `Controller`, `Adapter`, `linear_probe_r2`, `train_freeze_adapt`.

**Tech Stack:** Python, PyTorch (CPU no servidor / MPS no Mac), HuggingFace `transformers`, numpy, pytest. Sem GPU dedicada. LLM ≤ 1.5B.

---

## File Structure

- `brain/models.py` — **estender**: `H_DIM=896`, `AdapterLLM` (LayerNorm→MLP→L2).
- `brain/latent_bus.py` — **novo**: `LWWRegister` (buffer 1-slot thread-safe) + `CortexThread`.
- `brain/rtc.py` — **novo**: `rtc_blend()` (peso por passo: freeze-`d` + decaimento exponencial) — função pura, fácil de testar.
- `brain/cortex_llm.py` — **novo**: `LLMCortex` com backends `stub|mps`, `scene_to_prompt`, forward→hidden[L]→pooling→`h`, mede `lat_ms`.
- `brain/bridge_async.py` — **novo**: `eval_policy_async()` com 3 condições (`oracle`/`naive`/`rtc`).
- `brain/train.py` — **estender**: `delay_steps` em `train_freeze_adapt`.
- `brain/probe.py` — **estender**: `sweep_layer_pooling()` (gatekeeper).
- `stage3/stage3_loop.py` — **novo (runner)**: latência, gatekeeper, treino, sweep de atraso, relatório.
- `tests/` — testes (servidor, backend `stub`).
- `requirements.txt` — **editar**: adicionar `transformers`.

Constantes de dimensão em `brain/models.py`.

---

## Task 1: `rtc_blend` — a matemática da ponte (função pura)

**Files:**
- Create: `brain/rtc.py`
- Test: `tests/test_rtc.py`

- [ ] **Step 1: Teste que falha**

```python
# tests/test_rtc.py
import numpy as np
from brain.rtc import rtc_blend

def test_freeze_first_d_then_decay():
    z_old = np.zeros(4, dtype=np.float32)
    z_new = np.ones(4, dtype=np.float32)
    # i < d: peso do z_new = 0 (congela o que ja executa = z_old)
    assert np.allclose(rtc_blend(z_old, z_new, i=0, d=3, tau=2.0), z_old)
    assert np.allclose(rtc_blend(z_old, z_new, i=2, d=3, tau=2.0), z_old)
    # i == d: comeca a transicao (peso do novo > 0)
    out_d = rtc_blend(z_old, z_new, i=3, d=3, tau=2.0)
    assert (out_d > z_old).all() and (out_d < z_new).all()
    # i grande: converge para z_new
    out_far = rtc_blend(z_old, z_new, i=50, d=3, tau=2.0)
    assert np.allclose(out_far, z_new, atol=1e-2)

def test_d_zero_is_immediate():
    z_old = np.zeros(4, dtype=np.float32); z_new = np.ones(4, dtype=np.float32)
    out = rtc_blend(z_old, z_new, i=0, d=0, tau=1.0)
    assert (out > z_old).all()
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `. .venv/bin/activate && pytest tests/test_rtc.py -v`
Expected: FAIL `ModuleNotFoundError: No module named 'brain.rtc'`.

- [ ] **Step 3: Implementar**

```python
# brain/rtc.py
import numpy as np

def rtc_blend(z_old, z_new, i, d, tau=3.0):
    """Peso do z_new no passo i apos a chegada do z_new.
    i < d  -> congela (peso 0; mantem o que ja esta em execucao).
    i >= d -> w = 1 - exp(-(i-d)/tau), transicao suave (soft-mask estilo RTC).
    """
    z_old = np.asarray(z_old, dtype=np.float32)
    z_new = np.asarray(z_new, dtype=np.float32)
    if i < d:
        w = 0.0
    else:
        w = 1.0 - np.exp(-(i - d) / max(tau, 1e-6))
    return (1.0 - w) * z_old + w * z_new
```

- [ ] **Step 4: Rodar e ver passar**

Run: `pytest tests/test_rtc.py -v` — Expected: 2 passed.

- [ ] **Step 5: Commit**

```bash
git add brain/rtc.py tests/test_rtc.py && git commit -m "feat(stage3): rtc_blend (freeze-d + decaimento exponencial)"
```

---

## Task 2: `AdapterLLM` — hidden da LLM → latente

**Files:**
- Modify: `brain/models.py`
- Test: `tests/test_adapter_llm.py`

- [ ] **Step 1: Teste que falha**

```python
# tests/test_adapter_llm.py
import torch
from brain.models import AdapterLLM, H_DIM, Z_DIM

def test_adapter_llm_shape_and_l2norm():
    a = AdapterLLM()
    h = torch.randn(5, H_DIM)
    z = a(h)
    assert z.shape == (5, Z_DIM)
    norms = z.norm(dim=-1)
    assert torch.allclose(norms, torch.ones(5), atol=1e-4)  # saida L2-normalizada
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `pytest tests/test_adapter_llm.py -v` — Expected: FAIL `ImportError: cannot import name 'AdapterLLM'`.

- [ ] **Step 3: Implementar (estender `brain/models.py`)**

Adicionar ao fim de `brain/models.py`:
```python
H_DIM = 896  # hidden da Qwen2.5-0.5B


class AdapterLLM(nn.Module):
    """hidden da LLM congelada -> z' (Z_DIM). LayerNorm na entrada (anti-outlier
    dimensions dos hidden de LLM) + L2-norm na saida (limita o quao OOD um latente
    velho fica quando a ponte o transporta defasado)."""

    def __init__(self, h_dim=H_DIM):
        super().__init__()
        self.norm = nn.LayerNorm(h_dim)
        self.net = nn.Sequential(nn.Linear(h_dim, HIDDEN), nn.GELU(), nn.Linear(HIDDEN, Z_DIM))

    def forward(self, h):
        z = self.net(self.norm(h))
        return torch.nn.functional.normalize(z, dim=-1)
```

- [ ] **Step 4: Rodar e ver passar**

Run: `pytest tests/test_adapter_llm.py -v` — Expected: 1 passed.

- [ ] **Step 5: Commit**

```bash
git add brain/models.py tests/test_adapter_llm.py && git commit -m "feat(stage3): AdapterLLM (hidden LLM -> latente, LayerNorm+L2)"
```

---

## Task 3: `LWWRegister` + `CortexThread` — o buffer assíncrono

**Files:**
- Create: `brain/latent_bus.py`
- Test: `tests/test_latent_bus.py`

- [ ] **Step 1: Teste que falha**

```python
# tests/test_latent_bus.py
import time
import numpy as np
from brain.latent_bus import LWWRegister, CortexThread

def test_lww_last_write_wins_and_prev():
    r = LWWRegister()
    assert r.read() == (None, None, 0)
    r.publish(np.array([1.0]))
    z, zp, gen = r.read()
    assert z[0] == 1.0 and zp[0] == 1.0 and gen == 1   # primeiro: prev = z
    r.publish(np.array([2.0]))
    z, zp, gen = r.read()
    assert z[0] == 2.0 and zp[0] == 1.0 and gen == 2   # prev guarda o anterior

def test_cortex_thread_publishes_without_blocking():
    scene_holder = {"scene": np.zeros(3, dtype=np.float32)}
    def get_scene(): return scene_holder["scene"]
    def cortex_fn(scene): time.sleep(0.02); return scene + 1.0  # 'lento'
    reg = LWWRegister()
    th = CortexThread(get_scene, cortex_fn, reg, cooldown=0.0)
    th.start()
    time.sleep(0.1)               # deixa publicar algumas vezes
    th.stop(); th.join(timeout=1)
    z, _, gen = reg.read()
    assert gen >= 1 and z is not None  # publicou em background
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `pytest tests/test_latent_bus.py -v` — Expected: FAIL `ModuleNotFoundError`.

- [ ] **Step 3: Implementar**

```python
# brain/latent_bus.py
import threading
import time


class LWWRegister:
    """Buffer de 1 slot, last-write-wins, thread-safe. Guarda z atual, z anterior
    (para o blend) e um contador de geracao."""

    def __init__(self):
        self._lock = threading.Lock()
        self._z = None
        self._z_prev = None
        self._gen = 0

    def publish(self, z):
        with self._lock:
            self._z_prev = self._z if self._z is not None else z
            self._z = z
            self._gen += 1

    def read(self):
        with self._lock:
            return self._z, self._z_prev, self._gen


class CortexThread(threading.Thread):
    """Roda o cortex lento em background: le a scene atual, computa o latente e
    publica no registrador. Nunca bloqueia o loop rapido."""

    def __init__(self, get_scene, cortex_fn, register, cooldown=0.0):
        super().__init__(daemon=True)
        self.get_scene = get_scene
        self.cortex_fn = cortex_fn
        self.register = register
        self.cooldown = cooldown
        self._stop = threading.Event()

    def run(self):
        while not self._stop.is_set():
            scene = self.get_scene()
            z = self.cortex_fn(scene)
            self.register.publish(z)
            if self.cooldown:
                time.sleep(self.cooldown)

    def stop(self):
        self._stop.set()
```

- [ ] **Step 4: Rodar e ver passar**

Run: `pytest tests/test_latent_bus.py -v` — Expected: 2 passed.

- [ ] **Step 5: Commit**

```bash
git add brain/latent_bus.py tests/test_latent_bus.py && git commit -m "feat(stage3): LWWRegister + CortexThread (buffer assincrono)"
```

---

## Task 4: `LLMCortex` com backend `stub` (testável no servidor)

**Files:**
- Create: `brain/cortex_llm.py`
- Test: `tests/test_cortex_llm_stub.py`

- [ ] **Step 1: Teste que falha**

```python
# tests/test_cortex_llm_stub.py
import numpy as np
from brain.cortex_llm import LLMCortex
from brain.models import H_DIM

def test_stub_backend_returns_hidden_and_latency():
    cx = LLMCortex(backend="stub", stub_latency_ms=5.0, seed=0)
    scene = np.zeros(16, dtype=np.float32)
    h = cx.hidden(scene)
    assert h.shape == (H_DIM,)
    assert cx.lat_ms >= 5.0           # respeitou a latencia simulada
    # determinismo: mesma scene -> mesmo hidden (stub e funcao fixa)
    assert np.allclose(h, cx.hidden(scene))

def test_scene_to_prompt_is_short_text():
    cx = LLMCortex(backend="stub")
    p = cx.scene_to_prompt(np.arange(16, dtype=np.float32) / 16.0)
    assert isinstance(p, str) and "scene" in p
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `pytest tests/test_cortex_llm_stub.py -v` — Expected: FAIL `ModuleNotFoundError`.

- [ ] **Step 3: Implementar**

```python
# brain/cortex_llm.py
import time
import numpy as np
import torch
import torch.nn as nn
from brain.models import H_DIM


def quantize_scene(scene):
    """16-D float -> texto curto e estruturado (inteiros = poucos tokens)."""
    q = (np.asarray(scene, dtype=np.float32) * 100).round().astype(int)
    return "scene: " + " ".join(str(int(v)) for v in q)


class LLMCortex:
    """Cortex congelado. backend='stub' (MLP fixo + sleep calibrado, roda no servidor
    sem LLM) | 'mps' (Qwen2.5-0.5B real no Mac). Sempre CONGELADO; mede lat_ms."""

    def __init__(self, backend="stub", model_name="Qwen/Qwen2.5-0.5B",
                 layer=None, pooling="last", stub_latency_ms=200.0, seed=0):
        self.backend = backend
        self.pooling = pooling
        self.lat_ms = 0.0
        self._stub_lat = stub_latency_ms / 1000.0
        if backend == "stub":
            g = torch.Generator().manual_seed(seed)
            self._proj = torch.randn(16, H_DIM, generator=g) / np.sqrt(16)
        elif backend == "mps":
            from transformers import AutoModelForCausalLM, AutoTokenizer
            self.tok = AutoTokenizer.from_pretrained(model_name)
            self.model = AutoModelForCausalLM.from_pretrained(
                model_name, torch_dtype=torch.float16, output_hidden_states=True,
            ).to("mps").eval()
            for p in self.model.parameters():
                p.requires_grad_(False)
            n = self.model.config.num_hidden_layers
            self.layer = layer if layer is not None else n // 2
        else:
            raise ValueError(backend)

    def scene_to_prompt(self, scene):
        return quantize_scene(scene)

    @torch.no_grad()
    def hidden(self, scene):
        t0 = time.perf_counter()
        if self.backend == "stub":
            x = torch.tensor(np.asarray(scene, dtype=np.float32))
            h = torch.tanh(x @ self._proj)            # funcao fixa de scene
            if self._stub_lat:
                time.sleep(self._stub_lat)
            out = h.numpy().astype(np.float32)
        else:
            enc = self.tok(self.scene_to_prompt(scene), return_tensors="pt").to("mps")
            hs = self.model(**enc).hidden_states[self.layer][0]   # (T, H)
            if self.pooling == "mean":
                h = hs.mean(0)
            else:
                idx = int(enc["attention_mask"].sum().item()) - 1
                h = hs[idx]
            out = h.float().cpu().numpy().astype(np.float32)
        self.lat_ms = (time.perf_counter() - t0) * 1000.0
        return out
```

- [ ] **Step 4: Rodar e ver passar**

Run: `pytest tests/test_cortex_llm_stub.py -v` — Expected: 2 passed.

- [ ] **Step 5: Commit**

```bash
git add brain/cortex_llm.py tests/test_cortex_llm_stub.py && git commit -m "feat(stage3): LLMCortex (backend stub testavel no servidor + mps real)"
```

---

## Task 5: `eval_policy_async` — o loop de duas velocidades com as 3 condições

**Files:**
- Create: `brain/bridge_async.py`
- Test: `tests/test_bridge_async.py`

- [ ] **Step 1: Teste que falha**

```python
# tests/test_bridge_async.py
import numpy as np
from brain.bridge_async import eval_policy_async

def _oracle_policy(pos, z, env):
    return np.clip(env.goal - pos, -1.0, 1.0)

def test_three_conditions_run_and_oracle_sync_high():
    # cortex_fn devolve o goal verdadeiro como "latente" (via env) -> politica = oraculo
    out = {}
    for cond in ("oracle", "naive", "rtc"):
        out[cond] = eval_policy_async(
            policy_fn=_oracle_policy, cortex_fn=lambda scene: np.zeros(2, dtype=np.float32),
            condition=cond, d=5, n_episodes=10, seed=7, pass_env=True,
            env_kwargs={"switch_every": 200, "episode_len": 60})
    # com objetivo estatico e politica-oraculo, todas as condicoes terminam (smoke + teto)
    assert out["oracle"]["success"] >= 0.8
    assert set(out["rtc"].keys()) == {"success", "return"}
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `pytest tests/test_bridge_async.py -v` — Expected: FAIL `ModuleNotFoundError`.

- [ ] **Step 3: Implementar**

```python
# brain/bridge_async.py
import numpy as np
from brain.envs.contextual_reach import ContextualReach
from brain.rtc import rtc_blend

SUCCESS_DIST = 0.10


def eval_policy_async(policy_fn, cortex_fn, condition="rtc", d=5, tau=3.0,
                      n_episodes=50, seed=999, pass_env=False,
                      env_kwargs=None, success_dist=SUCCESS_DIST):
    """Loop de duas velocidades, latencia modelada como atraso de d passos na
    chegada do latente (versao deterministica para CI; a versao com thread real
    vive no runner do Mac). Condicoes:
      oracle -> latente sempre fresco (d efetivo 0);
      naive  -> usa o latente que chegou ha d passos, troca abrupta (ZOH);
      rtc    -> blend exponencial entre o latente anterior e o novo (freeze-d).
    """
    env = ContextualReach(seed=seed, **(env_kwargs or {}))
    total_ret, succ = 0.0, 0
    for ep in range(n_episodes):
        obs = env.reset()
        done = False
        hist = []          # historico de latentes computados (um por passo)
        z_used_prev = None
        step = 0
        ep_ret = 0.0
        reached = False
        while not done:
            z_now = np.asarray(cortex_fn(obs["scene"]), dtype=np.float32)
            hist.append(z_now)
            if condition == "oracle":
                z_eff = z_now
            else:
                z_arrived = hist[max(0, step - d)]      # latente atrasado de d passos
                if condition == "naive" or z_used_prev is None:
                    z_eff = z_arrived
                else:                                    # rtc: blend desde a chegada
                    age = 0 if step < d else (step % max(d, 1))
                    z_eff = rtc_blend(z_used_prev, z_arrived, i=age, d=0, tau=tau)
            z_used_prev = z_eff
            a = policy_fn(obs["pos"], z_eff, env) if pass_env else policy_fn(obs["pos"], z_eff)
            obs, r, done = env.step(a)
            ep_ret += r
            if -r <= success_dist:
                reached = True
            step += 1
        total_ret += ep_ret
        succ += int(reached)
    return {"success": succ / n_episodes, "return": total_ret / n_episodes}
```

> Nota: esta versão modela a latência como **atraso determinístico de `d` passos** (para CI reproduzível). O runner do Mac (Task 8) usa a `CortexThread` real para a latência wall-clock; as duas formas (real vs injetada) são reportadas separadamente, como manda o design.

- [ ] **Step 4: Rodar e ver passar**

Run: `pytest tests/test_bridge_async.py -v` — Expected: 1 passed.

- [ ] **Step 5: Commit**

```bash
git add brain/bridge_async.py tests/test_bridge_async.py && git commit -m "feat(stage3): eval_policy_async (3 condicoes: oracle/naive/rtc, atraso deterministico)"
```

---

## Task 6: `delay_steps` no treino + `sweep_layer_pooling` (gatekeeper)

**Files:**
- Modify: `brain/train.py`, `brain/probe.py`
- Test: `tests/test_stage3_train_probe.py`

- [ ] **Step 1: Teste que falha**

```python
# tests/test_stage3_train_probe.py
import torch
from brain.data import collect_oracle_dataset
from brain.train import train_freeze_adapt_llm
from brain.models import Cortex
from brain.probe import linear_probe_r2

def test_train_freeze_adapt_llm_runs_with_hidden():
    # usa um "hidden" sintetico de dim H_DIM no lugar da LLM (stub)
    from brain.models import H_DIM, AdapterLLM, Controller
    data = collect_oracle_dataset(n_episodes=20, seed=0)
    H = torch.randn(data["scene"].shape[0], H_DIM)      # hidden fixo por amostra
    (adapter, controller), losses = train_freeze_adapt_llm(H, data, epochs=40, seed=0)
    assert losses[-1] < losses[0]
    assert isinstance(adapter, AdapterLLM) and isinstance(controller, Controller)
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `pytest tests/test_stage3_train_probe.py -v` — Expected: FAIL `ImportError: train_freeze_adapt_llm`.

- [ ] **Step 3: Implementar — adicionar a `brain/train.py`**

```python
def train_freeze_adapt_llm(H, data, epochs=200, lr=1e-3, seed=0, delay_steps=0):
    """Treina AdapterLLM + Controller sobre hidden CONGELADO H (N, H_DIM), imitando
    o oraculo. delay_steps>0 serve o latente defasado durante o treino (robustez por
    construcao, truque do Helix)."""
    import torch
    from brain.models import AdapterLLM, Controller
    torch.manual_seed(seed)
    adapter, controller = AdapterLLM(), Controller()
    opt = torch.optim.Adam(list(adapter.parameters()) + list(controller.parameters()), lr=lr)
    loss_fn = torch.nn.MSELoss()
    pos, act = data["pos"], data["action"]
    losses = []
    for _ in range(epochs):
        opt.zero_grad()
        z = adapter(H)
        if delay_steps > 0:
            z = torch.roll(z, shifts=delay_steps, dims=0)   # latente "velho"
        pred = controller(pos, z)
        loss = loss_fn(pred, act)
        loss.backward(); opt.step()
        losses.append(loss.item())
    return (adapter, controller), losses
```

- [ ] **Step 4: Implementar — adicionar a `brain/probe.py`**

```python
def sweep_layer_pooling(hidden_by_layer, data, layers, poolings=("last", "mean")):
    """Gatekeeper: dado um dict {(layer,pooling): H (N,H_DIM)}, retorna o melhor R²
    de decodificar o goal, e a config vencedora. Reusa linear_probe_r2 via um
    'cortex' identidade que devolve o H pre-computado."""
    best = {"r2": -1e9, "layer": None, "pooling": None}
    for layer in layers:
        for pool in poolings:
            H = hidden_by_layer[(layer, pool)]
            ident = type("Id", (), {"__call__": staticmethod(lambda scene: scene)})()
            # linear_probe_r2 espera (frozen_cortex, data) e usa data['scene']/['goal'];
            # aqui passamos H diretamente via wrapper:
            r2 = _probe_r2_on_features(H, data["goal"])
            if r2 > best["r2"]:
                best = {"r2": float(r2), "layer": layer, "pooling": pool}
    return best


def _probe_r2_on_features(H, G, test_frac=0.25, seed=0):
    import torch
    n = H.shape[0]; nt = max(1, int(n * test_frac))
    g = torch.Generator().manual_seed(seed); perm = torch.randperm(n, generator=g)
    te, tr = perm[:nt], perm[nt:]
    X = lambda idx: torch.cat([H[idx], torch.ones(len(idx), 1)], dim=1)
    sol = torch.linalg.lstsq(X(tr), G[tr]).solution
    pred = X(te) @ sol
    ss_res = ((G[te] - pred) ** 2).sum(); ss_tot = ((G[te] - G[te].mean(0)) ** 2).sum()
    return float(1 - ss_res / ss_tot)
```

- [ ] **Step 5: Rodar e ver passar**

Run: `pytest tests/test_stage3_train_probe.py -v` — Expected: 1 passed.

- [ ] **Step 6: Commit**

```bash
git add brain/train.py brain/probe.py tests/test_stage3_train_probe.py && git commit -m "feat(stage3): train_freeze_adapt_llm (delay_steps) + sweep_layer_pooling (gatekeeper)"
```

---

## Task 7: Runner `stage3_loop.py` + suíte verde no servidor (stub)

**Files:**
- Create: `stage3/stage3_loop.py`
- Test: `tests/test_stage3_smoke.py`

- [ ] **Step 1: Teste que falha**

```python
# tests/test_stage3_smoke.py
from stage3.stage3_loop import run_stage3

def test_run_stage3_stub_smoke():
    # backend stub, rapido: roda o pipeline inteiro sem LLM
    res = run_stage3(backend="stub", n_episodes_data=20, epochs=40,
                     eval_episodes=15, ds=(0, 5, 15), seed=0)
    assert set(res.keys()) >= {"latency_ms", "gate_r2", "curve"}
    for cond in ("oracle", "naive", "rtc"):
        assert cond in res["curve"]
        assert set(res["curve"][cond].keys()) == {0, 5, 15}
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `pytest tests/test_stage3_smoke.py -v` — Expected: FAIL `ModuleNotFoundError`.

- [ ] **Step 3: Implementar `stage3/stage3_loop.py`**

```python
# stage3/stage3_loop.py
import json
import os
import numpy as np
import torch
from brain.data import collect_oracle_dataset
from brain.cortex_llm import LLMCortex
from brain.models import H_DIM
from brain.train import train_freeze_adapt_llm
from brain.probe import _probe_r2_on_features
from brain.bridge_async import eval_policy_async


def _hidden_matrix(cortex, data):
    H = np.stack([cortex.hidden(s.numpy()) for s in data["scene"]])
    return torch.tensor(H, dtype=torch.float32)


def run_stage3(backend="stub", model_name="Qwen/Qwen2.5-0.5B", layer=None,
               pooling="last", n_episodes_data=200, epochs=200, eval_episodes=50,
               ds=(0, 2, 5, 10, 15, 20, 30, 50), task=None, success_dist=0.10, seed=0):
    task = task or {"switch_every": 20, "episode_len": 100}
    cortex = LLMCortex(backend=backend, model_name=model_name, layer=layer,
                       pooling=pooling, stub_latency_ms=200.0, seed=seed)
    data = collect_oracle_dataset(n_episodes=n_episodes_data, seed=seed, env_kwargs=task)

    # latencia medida (sobre algumas chamadas)
    lats = []
    for s in data["scene"][:20]:
        cortex.hidden(s.numpy()); lats.append(cortex.lat_ms)
    latency_ms = {"L50": float(np.percentile(lats, 50)), "L95": float(np.percentile(lats, 95))}

    # hidden congelado + gatekeeper (R2 do goal)
    H = _hidden_matrix(cortex, data)
    gate_r2 = _probe_r2_on_features(H, data["goal"])

    # treina adapter+controller sobre o hidden congelado
    (adapter, controller), _ = train_freeze_adapt_llm(H, data, epochs=epochs, seed=seed)
    adapter.eval(); controller.eval()

    @torch.no_grad()
    def cortex_fn(scene):
        h = torch.tensor(cortex.hidden(scene)).unsqueeze(0)
        return adapter(h).squeeze(0).numpy()

    @torch.no_grad()
    def policy_fn(pos, z):
        return controller(torch.tensor(pos).unsqueeze(0),
                          torch.tensor(z, dtype=torch.float32).unsqueeze(0)).squeeze(0).numpy()

    curve = {c: {} for c in ("oracle", "naive", "rtc")}
    for cond in curve:
        for d in ds:
            m = eval_policy_async(policy_fn, cortex_fn, condition=cond, d=d,
                                  n_episodes=eval_episodes, seed=10_000 + d,
                                  env_kwargs=task, success_dist=success_dist)
            curve[cond][d] = m["success"]

    res = {"backend": backend, "latency_ms": latency_ms, "gate_r2": round(gate_r2, 3),
           "curve": curve, "ds": list(ds)}
    os.makedirs("results", exist_ok=True)
    json.dump(res, open("results/stage3.json", "w"), indent=2)
    return res


def main():
    import sys
    backend = sys.argv[1] if len(sys.argv) > 1 else "stub"
    res = run_stage3(backend=backend)
    print(json.dumps({k: res[k] for k in ("backend", "latency_ms", "gate_r2")}, indent=2))
    print("\nsucesso x atraso (d):")
    for cond in ("oracle", "naive", "rtc"):
        print(f"  {cond:7s} " + "  ".join(f"d={d}:{res['curve'][cond][d]:.2f}" for d in res["ds"]))
    print("\nresults/stage3.json salvo")


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: Rodar e ver passar + suíte completa**

Run: `pytest -q` — Expected: todos verdes.
Run: `. .venv/bin/activate && python -m stage3.stage3_loop stub` — Expected: imprime latência (stub ~200ms), gate_r2 (stub alto), e a curva sucesso×d. Sanity: `naive` cai com d grande; `rtc` cai menos.

- [ ] **Step 5: Commit**

```bash
git add stage3/stage3_loop.py tests/test_stage3_smoke.py && git commit -m "feat(stage3): runner (latencia, gatekeeper, treino, curva sucesso x atraso)"
```

---

## Task 8: Execução REAL no Mac (Qwen2.5-0.5B, MPS)

**Files:** nenhum novo — roda o que foi construído, no Mac, via SSH.

- [ ] **Step 1: Sincronizar o código pro Mac e instalar `transformers`**

```bash
# do servidor:
ssh mac 'mkdir -p ~/brain_stage3'
rsync -az --exclude .venv --exclude .git --exclude results /home/savino/projects/brain/ mac:~/brain_stage3/   # ou scp dos arquivos brain/, stage3/, tests/
ssh mac '~/stage2_probe/.venv/bin/pip install -q transformers && echo TRANSFORMERS_OK'
```
(Reusa o venv do estágio 2; já tem torch/MPS.)

- [ ] **Step 2: Gatekeeper no modelo REAL (antes de tudo)**

Run:
```bash
ssh mac 'cd ~/brain_stage3 && ~/stage2_probe/.venv/bin/python -c "
from brain.cortex_llm import LLMCortex
from brain.data import collect_oracle_dataset
from brain.probe import _probe_r2_on_features
import torch, numpy as np
cx=LLMCortex(backend=\"mps\")
data=collect_oracle_dataset(n_episodes=60, seed=0, env_kwargs={\"switch_every\":200,\"episode_len\":80})
H=torch.tensor(np.stack([cx.hidden(s.numpy()) for s in data[\"scene\"]]),dtype=torch.float32)
print(\"lat_ms\", round(cx.lat_ms,1), \"gate_r2\", round(_probe_r2_on_features(H,data[\"goal\"]),3))
"'
```
Expected: imprime a **latência real** do forward da Qwen no MPS e o **R² do gatekeeper**.
- Se `gate_r2 ≥ 0.7` → seguir.
- Se baixo → **parar e ajustar** (camada/pooling via `sweep_layer_pooling`, ou refinar `quantize_scene`) antes do loop. Não prosseguir com info destruída.

- [ ] **Step 3: Calibrar o estressor pela latência medida**

Com a latência `L` (ms) e `fps` do FastLoop, estimar `d = L·fps` e setar `switch_every ≈ 1–3·d` no `task` (senão `naive` não quebra e o experimento perde poder). Anotar os valores.

- [ ] **Step 4: Rodar o experimento real no Mac**

Run:
```bash
ssh mac 'cd ~/brain_stage3 && ~/stage2_probe/.venv/bin/python -m stage3.stage3_loop mps'
```
Expected: latência real (L50/L95), gate_r2, e a **curva sucesso × atraso** para `oracle`/`naive`/`rtc`. Salva `results/stage3.json` no Mac.

- [ ] **Step 5: Trazer o resultado e gerar o veredito**

```bash
scp mac:~/brain_stage3/results/stage3.json /home/savino/projects/brain/stage3/result-mac-stage3.json
```
Ler a curva e classificar o veredito:
- **(A) co-controlador:** `rtc` mantém ≥90% de `oracle` num regime onde `naive` cai abaixo de ~50%.
- **(B) planejador lento:** `rtc` quebra em `d` pequeno → acionar Plano B (LLM emite sub-objetivos esparsos: `switch_every` ≫ d, cadência do córtex menor) e medir se o sucesso volta.

- [ ] **Step 6: Relatório + commit**

Escrever `docs/superpowers/RESULTADO-stage3.md` (números de latência, curva, veredito A/B, e a nota honesta: blend é *adaptação* de RTC, não RTC literal; quais d foram medidos vs injetados). Commit.

```bash
git add stage3/result-mac-stage3.json docs/superpowers/RESULTADO-stage3.md && git commit -m "feat(stage3): resultado real no Mac (curva sucesso x latencia, veredito)"
```

---

## Self-Review

**Spec coverage (design §1–7):** rtc_blend (Task 1) ↔ §2.6; AdapterLLM (Task 2) ↔ §2.3; LWWRegister/CortexThread (Task 3) ↔ §2.6; LLMCortex stub+mps (Task 4) ↔ §2.1/2.2; eval_policy_async 3 condições (Task 5) ↔ §4; delay_steps + sweep gatekeeper (Task 6) ↔ §2.2/§4 passo 0; runner (Task 7) ↔ §3; execução Mac + veredito A/B (Task 8) ↔ §4/§6/§7. Latência-sob-carga e `fps` da thread: medidos no Task 8 / verificáveis; o teste de não-bloqueio está no Task 3.

**Placeholder scan:** sem TODO/TBD; cada step tem código ou comando completo.

**Type consistency:** `H_DIM=896`, `Z_DIM=8`; `AdapterLLM(h)->(*,Z_DIM)`; `Controller(pos,z)` inalterado; `eval_policy_async(policy_fn, cortex_fn, condition, d, ...)` chamado igual no runner e nos testes; `LLMCortex.hidden(scene)->(H_DIM,)` e `.lat_ms`; `run_stage3` retorna `{latency_ms, gate_r2, curve, ds, backend}` conforme o smoke test.

**Nota de escopo:** Tasks 1–7 são TDD no servidor (backend `stub`, sem LLM). Task 8 é a execução real no Mac (a única que precisa da LLM/MPS) e produz o entregável científico. A versão determinística de latência (atraso de `d` passos) é para CI reproduzível; a latência wall-clock real com `CortexThread` é exercida e medida no Task 8.
